# TODO: Add the resources for this module.
#
# Naming convention:
#   - Use `this` as the logical name when there is a single resource of a given
#     type (e.g., aws_s3_bucket.this).
#   - Use a descriptive name when there are multiple resources of the same type.
#
# Example:
# resource "aws_example_resource" "this" {
#   name = var.name
#   tags = var.tags
# }
